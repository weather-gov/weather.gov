# Routes

This directory contains the API route definitions and handlers.

Each subdirectory typically corresponds to a specific domain or endpoint family. The routes are aggregated and exported for use by the Fastify server.
