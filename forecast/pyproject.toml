[tool.ruff]
show-fixes = true
line-length = 120
exclude = [".venv", "manage.py", "backend/migrations/*","spatial/migrations/*"]

[tool.ruff.lint]
select = [
    # PyFlakes
    "F",
    # flake8
    "S", "B", "DJ", "DTZ", "Q", "INT", "RET", "ARG", "ASYNC", "COM", "T20", "PT",
    # pycodestyle
    "E", "W",
    # pydocstyle
    "D",
    # Pylint
    "PL",
    # pep8-naming
    "N",
    # flynt
    "FLY",
    # tryceratops
    "TRY",
    # isort
    "I",
    # Perflint
    "PERF",
    # mccabe
    "C90",
]
ignore = [
    # Whitespace before ':'
    "E203",
    # Missing docstring in public module
    "D100",
    # Missing docstring in public package
    "D104",
    # Missing docstring in magic method
    "D105",
    # Missing docstring in `__init__`
    "D107",
    # Use a regular `assert` instead of unittest-style `assertEqual`
    "PT009",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    # Module imported but unused
    "F401",
    # 'from module import *' used
    "F403",
    # Module level import not at top of file
    "E402"
]

[tool.ruff.lint.pydocstyle]
convention = "pep257"

[tool.ruff.lint.flake8-unused-arguments]
ignore-variadic-names = true

[tool.djlint]
# H030 Consider adding a meta description.
# H031 Consider adding meta keywords.
# H006 img tag should have height and width attributes.
# H023 Do not use entity references.
# H020 Empty tag pair found. Consider removing.
# H021 Inline styles should be avoided.
ignore = "H030,H031,H006,H023,H020,H021"
indent = 2

[tool.mypy]
ignore_missing_imports = true
